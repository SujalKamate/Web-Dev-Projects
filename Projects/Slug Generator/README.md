# Slug Generator

A simple slug generator for creating URL-friendly slugs from text input.

## Features

- A simple interface which provides the user with a text box to enter a title for which they need a slug to be generated.
- A copy button which copies the output.

## Run it

Open `index.html` in any modern browser.
